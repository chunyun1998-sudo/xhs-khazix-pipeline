---
name: "xhs-khazix-pipeline"
description: "小红书长文写作标准化工作流。按步骤执行：选题→搜索素材→立意阐述→用户确认立意→自动搜索素材+写稿→去AI味→敏感词检验→小标题提案→用户确认小标题→大标题5选1→用户选择→摘要提案→用户确认摘要→摘要插入文档开头。每一步有明确的用户确认节点，不可跳过。"
---

# 小红书长文标准化工作流

## 适用场景

用户提供选题（通常是 AI 热点或商业财经热点），需要按标准化流程完成一篇卡兹克（Khazix）风格的小红书长文。每次启动本 Skill 时，必须严格按以下步骤顺序执行，每个环节都有明确的用户确认节点。

## 前置约定

- **文件命名格式**：`✕✕✕_日期_版本号.md`（由 AI 根据文章主题自动拟定）
- **默认文件存放路径**：`/Users/chunyun/Library/Mobile Documents/com~apple~CloudDocs/Hermes Agent 交付文件/小红书长文系列/`
- **写作风格**：卡兹克（Khazix）风格，详细参考 `khazix-writer` Skill 的风格指引
- **调用 khazix-writer**：在 Step 4 撰稿时自动调用 khazix-writer Skill 加载其风格规则
- **每步执行完必须等待用户确认或选择后，才能进入下一步**
- **可视化交互**：所有用户确认节点优先使用 `AskUserQuestion` 工具提供按钮式选择，用户点击按钮即可，无需打字输入

---

## 标准化工作流

### Step 1 — 接收选题

用户提供一个选题方向，通常来自：
- AI 热点新闻（来自 AI HOT 资讯）
- 商业财经热点
- 用户自己的灵感

→ 确认收到选题，进入 Step 2。

### Step 2 — 搜索素材

针对用户提供的选题，执行网络搜索。搜索范围包括：
- 事件核心事实（时间、人物、数据）
- 多方信源交叉验证
- 评论区/舆论反应（如果有）

输出：整理成结构化的素材摘要，包含关键数据点和来源链接。

→ 素材整理完成后，进入 Step 3。

### Step 3 — 阐述立意 + 可视化确认

基于收集到的素材，给用户提出 **2-3 个** 可选的文章立意方向。每个方向需说明：
- 核心角度（一句话概括）
- 为什么这个角度有意思
- 潜在的风险或需要注意的陷阱（如有）

然后用 `AskUserQuestion` 工具发起交互式选择：
- **question**：请选择你感兴趣的立意方向
- **header**：选择立意
- **options**：每个方向用一个按钮，label 写方向简称（如"方向A：对位追赶"），description 写核心角度一句话
- **multiSelect**：false

用户点击按钮或输入其他内容后，根据选择进入对应的方向执行。

→ 用户确认立意后，进入 Step 4。

### Step 4 — 自动搜索素材 + 撰稿

根据用户确认的立意方向，自动进行补充素材搜索，确保文章有足够的具体数据、案例和细节支撑。

然后调用 khazix-writer Skill 加载风格规则，按卡兹克风格撰写长文：
- 开头从具体事件/场景切入，不宏大叙事
- 段落短，一句话可以成段
- 禁用冒号、破折号、双引号
- 禁用 AI 套话（说白了、意味着什么、本质上、换句话说、不可否认）
- 避免"不是……而是……"句式
- 有具体的场景、人物、数据支撑观点
- 保持"有见识的普通人在认真聊一件打动他的事"的调性

输出：完整文章初稿。

→ 撰稿完成后，进入 Step 5。

### Step 5 — 去 AI 味（自动执行）

对全文执行去 AI 味处理：
- 扫描并替换禁用词
- 扫描并替换禁用标点
- 拆解三段式排比、金句收尾、教科书转场等 AI 标志结构
- 增加口语化表达和活人感
- 修复"不是……而是……"风格

完成后输出质检报告。

→ 去 AI 味完成后，进入 Step 6。

### Step 6 — 敏感词检验（自动执行）

对全文进行小红书敏感词扫描，重点关注：
- 金融数据（具体金额、涨跌百分比、市场走势负面定性）
- 中美对比/地缘政治表述
- 行业负面判断（终结、崩溃、危机等）
- 绝对化表述

对高风险内容给出修改建议并自动执行替换。

→ 敏感词检验完成后，进入 Step 7。

### Step 7 — 小标题提案 + 可视化确认

根据文章的情绪弧线和内容结构，给出 **4 个** 小标题建议（注意：AskUserQuestion 最多支持 4 个按钮，所以从 4-5 个精简为 4 个）。每个小标题要求：
- 简短有力，2-6 个字为佳
- 点出该段的核心情绪或观点
- 与前后小标题形成递进或转折关系
- 按文章叙事顺序排列

然后在文字中列出 4 个小标题，并用 `AskUserQuestion` 工具发起交互式确认：
- **question**：请确认是否按以上小标题方案执行
- **header**：确认小标题
- **options**：选项一为「确认，插入」，选项二为「重列小标题」
- **multiSelect**：false

如果用户选「确认」，直接插入文章正文。如果选「重列」，回到本步骤重新提案。

→ 用户确认后，将小标题插入文章正文对应位置，进入 Step 8。

### Step 8 — 大标题提案 + 可视化选择

根据文章的核心立意，列出 **5 个大标题选项**。每个选项需是完整的标题句式。要求：
- 客观、严谨、深度报道风格
- 避免情绪化表达和夸张用词
- 信息量充足，能让人一眼知道文章在讲什么
- 覆盖不同角度（事件驱动、悬念驱动、对比驱动等）

在文字中完整列出全部 5 个大标题，然后用 `AskUserQuestion` 工具发起交互式选择：
- **question**：请选择你想要的标题
- **header**：选择大标题
- **options**：**前 4 个** 标题各用一个按钮，label 写标题的核心短语（不超过 8 个字），description 写原标题完整句式
- 第 5 个标题以文字形式标注「如果你选第5个，请在输入框中打 5」
- **multiSelect**：false

用户点击按钮选择前 4 个之一，或在输入框中填 5 选第 5 个。无论哪种方式，均视为已选择。用户还可能在输入框中要求结合重列（如"把2和4结合一下"），此时回到本步骤重新生成。

→ 用户确认标题后，插入到文档最顶部作为文章大标题，进入 Step 9。

### Step 9 — 摘要写作 + 可视化确认

根据文章内容，撰写一段约 100 字的深度报道风格摘要。要求：
- 把握文章核心利益和信息密度
- 带有吸引人的钩子
- 深度报道风格（正式、严谨、有信息量）
- 不是小红书文案风

在文字中展示摘要全文，然后用 `AskUserQuestion` 工具发起交互式确认：
- **question**：请确认是否使用以上摘要
- **header**：确认摘要
- **options**：选项一为「确认，插入文档开头」，选项二为「重写摘要」
- **multiSelect**：false

如果用户选「重写摘要」，回到本步骤重新撰写。
如果用户要求调整方向，也回到本步骤按新方向重写。

→ 用户确认后，进入 Step 10。

### Step 10 — 摘要插入文档开头

将最终确认的摘要以引用格式（`> ...`）插入到文档的**开头位置**（大标题下方、正文第一段上方）。

→ 全部流程完成。

---

## 流程图总览

```text
用户提供选题
    ↓
Step 1 — 接收选题
    ↓
Step 2 — 搜索素材
    ↓
Step 3 — 阐述立意 → 🟦 按钮选择
    ↓
Step 4 — 自动搜索 + 撰稿（调用 khazix-writer）
    ↓
Step 5 — 去 AI 味（自动）
    ↓
Step 6 — 敏感词检验（自动）
    ↓
Step 7 — 小标题提案 → 🟦 按钮确认/重列
    ↓
Step 8 — 大标题提案（5选1→前4按钮+第5文字） → 🟦 按钮选择
    ↓
Step 9 — 摘要写作 → 🟦 按钮确认/重写
    ↓
Step 10 — 摘要插入文档开头
    ↓
✅ 完成
```

## 可视化交互说明

本 Skill 在 4 个用户确认节点使用 `AskUserQuestion` 工具提供按钮式交互。工具规格：

- **问题数**：每次只问 1 个问题
- **选项数**：2-4 个按钮（工具上限为 4），超出部分以文字说明 + 「其他」输入框兜底
- **多选**：全部设为 false（单选题）
- **按钮 label**：控制在 8 个字以内，作为选项标签
- **按钮 description**：一句话解释该选项的含义或效果

### 各节点选项配置速查

| 节点 | 问题 | options | 自定义输入用途 |
|---|---|---|---|
| Step 3 选择立意 | 请选择你感兴趣的立意方向 | 2-3 个方向标签 | 用户提出自创方向 |
| Step 7 确认小标题 | 请确认是否按以上小标题方案执行 | 「确认，插入」「重列小标题」 | 用户提出修改意见 |
| Step 8 选择大标题 | 请选择你想要的标题 | 前 4 个标题标签 | 选第5个 / 要求结合重列 |
| Step 9 确认摘要 | 请确认是否使用以上摘要 | 「确认，插入文档开头」「重写摘要」 | 用户提出调整方向 |

## 注意事项

1. **每一步完成后必须停顿**，等待用户指令再进入下一步。不要自动连续执行。
2. Step 2（搜索素材）可引用 AI HOT 数据或其他网络信源，数据需交叉验证。
3. Step 4 写稿时自动调用 khazix-writer Skill 加载风格规则。
4. 标题和摘要如果用户要求修改，需回到对应步骤重新执行，修改确认后再继续。
5. 文件保存路径始终为工作区文件夹，不要存入临时目录。
6. **可视化交互仅限于 AskUserQuestion 工具的能力范围**，不支持自定义 UI 组件、富文本渲染或嵌入式预览。
